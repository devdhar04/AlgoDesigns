on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Hello world action step
      id: hello
      uses: devdhar04/jacoco-report
      with:
       repotoken: ${{ secrets.GITHUB_TOKEN }}
